# SecondMe OAuth (fill in your values)
SECONDME_CLIENT_ID=
SECONDME_CLIENT_SECRET=
SECONDME_REDIRECT_URI=http://localhost:3000/api/auth/callback

# SecondMe API Configuration
SECONDME_API_BASE_URL=https://app.mindos.com/gate/lab
SECONDME_OAUTH_URL=https://go.second.me/oauth/

# Session Encryption (required - min 32 chars, generate with: openssl rand -base64 32)
SESSION_SECRET=

# OAuth State Validation Mode
# true = strict mode (default), exact state match required
# false = loose mode, for WebView scenarios where state may be modified
OAUTH_STATE_STRICT=true

# App
NEXT_PUBLIC_BASE_URL=http://localhost:3000
